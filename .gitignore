node_modules/
dist/
*.log
.env
.env.local
.DS_Store
.idea/
.vscode/*
# But DO commit project-shared debug configs.
!.vscode/launch.json*
# But DO commit project-shared debug configs.
!.vscode/launch.json

# Session histories — per-project, contain user conversation, never commit
.invox/

# Stray scratch files from manual Zed sessions / sandbox tests
sorting.txt
convert_time.ps1
convert_time.py
.claude
.codegraph

# CodeBuddy IDE 本地元数据（每台机器自己生成，不入库）
.codebuddy/
