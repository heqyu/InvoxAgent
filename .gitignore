node_modules/
dist/
*.log
.env
.env.local
.DS_Store
.idea/
.vscode/*
# But DO commit project-shared debug configs.
!.vscode/launch.json*
# But DO commit project-shared debug configs.
!.vscode/launch.json

# Machine-local state (sessions contain user conversations, invox.env has per-host config)
.invox/sessions/
.invox/invox.env
.invox/providers.json

# Stray scratch files from manual Zed sessions / sandbox tests
sorting.txt
convert_time.ps1
convert_time.py
.claude
.codegraph

# CodeBuddy IDE 本地元数据（每台机器自己生成，不入库）
.codebuddy/

# 本机启动脚本（含本机 API Key 等敏感配置）
start-server.bat
start-server.ps1

# Windows 误操作残留
nul
docs/diagrams/
